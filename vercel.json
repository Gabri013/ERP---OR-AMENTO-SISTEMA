{
  "version": 2,
  "buildCommand": "cd api-server && npm install && npm run build && cd ../sistema-os && npm ci && npm run build",
  "outputDirectory": "sistema-os/dist/public",
  "installCommand": "echo 'Root has no package.json - frontend npm install runs in buildCommand'",
  "devCommand": "cd sistema-os && npm run dev",
  "regions": ["iad1"],
  "functions": {
    "api-server/dist/index.mjs": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api-server/dist/index.mjs"
    }
  ]
}
